/*
   This file contains routes names created by next using file names in
    directory "pages".

    It is used and appended to dynamic routes such as "/product/[random-id]"
    so that dynamic routes don't have to be changed globally but in this 
    one file in a case where the corresponding file name in pages changes.
*/

export default {
  account: 'account',
  cart: 'cart',
  login: 'login',
  picks: 'picks',
  product: 'product',
  savedItem: 'saved-item',
  search: 'search',
  signup: 'signup',
  terms: 'terms'
};
